{
  "name": "resume",
  "version": "1.0.0",
  "description": "Resume",
  "main": "resume.json",
  "scripts": {
    "export:html": "resume export index --theme kwan --format html",
    "export:pdf": "resume export index --theme kwan --format pdf",
    "export": "npm run export:html && npm run export:pdf",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Kenneth Aasan",
  "license": "MIT",
  "dependencies": {
    "jsonresume-theme-kwan": "0.0.2",
    "resume-cli": "^0.4.19"
  }
}
